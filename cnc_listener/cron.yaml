cron:
  - description: "daily subfolder job"
    url: /tasks/subfolder/
    schedule: every 1 hours
    target: cnc-listener
 
  - description: "retry failed inserts and uploads"
    url: /tasks/retry/
    schedule: every 3 minutes
    target: cnc-listener

